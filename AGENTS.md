# AGENTS.md

## 项目目标

本项目是用户的日语学习笔记库，学习目标是通过日语能力考试 JLPT N1。主数据文件是 `日语学习笔记.out`，使用 Fractal Note 的 JSON 格式保存，可在 VS Code 中以大纲或脑图查看。

所有讲解应在保证准确的前提下面向 N1 备考：不仅帮助理解当前句子，还要帮助用户积累词汇、辨析语法、理解语气，并识别考试中容易出现的近义表达和易错点。

## Agent 工作规则

1. 用户明确要求“加入笔记”、“记下来”、“沉淀”或同等操作时，才修改 `.out` 文件。
2. 普通的翻译或语法问答不自动写入文件，除非用户要求记录。
3. 新增前先检查是否已有同一词汇、语法或原句；有重复时补充原节点，不另建重复节点。
4. 尽量保持节点简短，便于脑图浏览。详细补充放在 `subtext` 或子节点中。
5. 不随意改动用户手工调整的文字、标签、折叠状态和节点顺序。
6. 除非用户明确要求，不将主笔记转换为其他格式。

## 内容标准

### 词汇

至少记录：

- 日语写法
- 假名读音
- 自然中文释义
- 必要时补充语感、使用场景和例句

### 语法

尽量记录：

- 语法形式
- 接续方式
- 核心意思
- 语气和使用场景
- 一个简短例句及翻译
- 常见口语缩约或易错点

### 句子解析

凡是用户要求解析日语句子，回答必须同时讲解词汇和语法，不得只给出翻译。记入笔记时也遵循同一标准。

每次句子解析至少包含：

- 日语原句
- 假名读音
- 自然中文翻译
- 词汇讲解：读音、词性、句中含义，以及必要的搭配或语感
- 语法讲解：接续、意思、句中作用和语气
- 句子成分与修饰关系拆解
- 省略、语气和上下文说明

面向 N1 备考时，视句子内容补充：

- N1 或容易混淆的语法项
- 近义词、近义句型之间的语气差异
- 书面语、口语、正式或随意程度
- 高频搭配、汉字读音和容易误读的词
- 可能的考题角度，但不为了套用级别而过度扩展

## `.out` 数据约束

- 顶层必须保留 `version`、`title`、`rootIds`、`nodes` 和 `searchFocusMode`。
- `rootIds` 中的每个 ID 必须存在于 `nodes`。
- `nodes` 的键必须与对应节点的 `id` 相同。
- 根节点的 `parentId` 为 `null`。
- 子节点的 `parentId` 必须指向父节点 ID。
- 父节点的 `children` 必须包含所有直接子节点 ID，并保持显示顺序。
- 新节点 ID 必须唯一、稳定，优先使用简短的 ASCII kebab-case 名称。
- 一般节点保留 `text`、`tags`、`collapsed`、`checked`、`subtext` 和 `images` 字段。
- 不创建与 `.out` 同名的资源目录，除非确实需要图片、附件或独立页面。

## 默认归类

- 单词和固定搭配 → `词汇`
- 句型、助词和表达方式 → `语法`
- 活用表和不规则变形 → `动词变形`
- 完整台词和长句 → `句子解析`
- 谐音、作品出处和日本文化背景 → `文化与趣味表达`

## 修改后检查

每次修改 `日语学习笔记.out` 后，至少确认：

1. JSON 可以正常解析。
2. 节点 ID 没有重复。
3. 所有子节点都存在。
4. 父子引用相互一致。
5. 日语汉字、假名、中文翻译和标点没有因编辑而损坏。
