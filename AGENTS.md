# AGENTS.md

## 项目目标

本项目是用户的日语学习笔记库，学习目标是通过日语能力考试 JLPT N1。主数据文件是 `日语学习笔记.md`，使用 Markdown 层级保存，可在 VS Code 中直接阅读和编辑。

所有讲解应在保证准确的前提下面向 N1 备考：不仅帮助理解当前句子，还要帮助用户积累词汇、辨析语法、理解语气，并识别考试中容易出现的近义表达和易错点。

## Agent 工作规则

1. 用户明确要求“加入笔记”、“记下来”、“沉淀”或同等操作时，才修改 `日语学习笔记.md`。
2. 普通的翻译或语法问答不自动写入文件，除非用户要求记录。
3. 新增前先检查是否已有同一词汇、语法或原句；有重复时补充原节点，不另建重复节点。
4. 尽量保持条目简短，便于浏览。详细补充放在同一列表项的续行或子列表中。
5. 不随意改动用户手工调整的文字、标题层级和条目顺序。
6. 除非用户明确要求，不将主笔记转换为其他格式。

## 内容标准

### 词汇

至少记录：

- 日语写法
- 假名读音
- 自然中文释义
- 必要时补充语感、使用场景和例句

### 语法

尽量记录：

- 语法形式
- 接续方式
- 核心意思
- 语气和使用场景
- 一个简短例句及翻译
- 常见口语缩约或易错点

### 句子解析

凡是用户要求解析日语句子，回答必须同时讲解词汇和语法，不得只给出翻译。记入笔记时也遵循同一标准。

每次句子解析至少包含：

- 开头先提供假名阅读辅助。
- 日语原句
- 自然中文翻译
- 词汇讲解：读音、词性、句中含义，以及必要的搭配或语感
- 语法讲解：接续、意思、句中作用和语气
- 句子成分与修饰关系拆解
- 省略、语气和上下文说明
- 难度提示：分别给出词汇、语法、阅读理解三个维度的大致 JLPT 难度水平，并简要说明判断依据

面向 N1 备考时，视句子内容补充：

- N1 或容易混淆的语法项
- 近义词、近义句型之间的语气差异
- 书面语、口语、正式或随意程度
- 高频搭配、汉字读音和容易误读的词
- 可能的考题角度，但不为了套用级别而过度扩展

### 注音

日语注音（包括句子解析开头的注音）须先读取并遵循项目内的 [japanese-furigana skill](.agents/skills/japanese-furigana/SKILL.md)，排版规则和模板统一在该 skill 中维护。

### 详细解析归档

- 仅在用户明确要求“归档”“存档”或同等操作时，新增或修改详细解析档案。
- `句子解析数据.json` 是详细归档的唯一内容源；每个条目须包含 `id`、`title`、`sentence`、`furigana`、`translation`、`vocabulary`、`grammar`、`structure`、`nuance` 和 `difficulty`。`structure` 的第一项固定为 `公式骨架`，用抽象符号呈现句法公式；后续项目保存成分拆解与说明，全部按同一列表样式渲染。
- `furigana` 统一使用显式青空文库式注音：`｜汉字《かな》`。每个注音范围都必须以 `｜` 开头，便于生成器稳定转换为上方假名。
- 不手工编辑 `句子解析档案.html`；完成 JSON 修改后，先用 `jq empty 句子解析数据.json` 校验，再运行 `python3 tools/build_sentence_archive.py` 生成 HTML。
- `templates/句子解析档案.css` 是阅读页样式源。修改样式后也必须重新运行生成器。
- `句子解析归档.md` 是历史 Markdown 档案，除非用户明确要求迁移或编辑，否则保留不动。

## 主笔记 Markdown 约定

- 顶层使用一个 `# 日语学习笔记` 标题，一级分类使用 `##`，有下级内容的词条使用 `###`。
- 普通词汇、语法和变形使用无序列表；补充说明放在同一列表项的续行，避免不必要的长段落。
- 保持现有标题层级、条目顺序、日语写法、假名和中文释义。
- 不创建与主笔记同名的资源目录，除非确实需要图片、附件或独立页面。

## 默认归类

- 单词和固定搭配 → `词汇`
- 句型、助词和表达方式 → `语法`
- 活用表和不规则变形 → `动词变形`
- 完整台词和长句 → `句子解析`
- 谐音、作品出处和日本文化背景 → `文化与趣味表达`

## 修改后检查

每次修改 `日语学习笔记.md` 后，至少确认：

1. Markdown 标题和列表层级完整、可读。
2. 日语汉字、假名、中文翻译和标点没有因编辑而损坏。
3. 新增内容位于正确分类，且没有重复条目。

曾出现过默认调用 `node` 但当前环境找不到命令的无效操作。若确实需要 `node` 或其他运行时，先用 `command -v` 确认命令存在，避免无效操作。
